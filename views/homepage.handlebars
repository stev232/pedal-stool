{{#each blogList as |blog| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{blog.title}}</h2>
  </div>
  <div class="col-md-6">
    <p>{{blog.content}}</p>
  </div>
</div>
{{/each }}
