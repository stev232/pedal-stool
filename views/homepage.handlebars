{{#each blogList as |blog| }}
<div class="row align-center mb-5">
  <a href="/blogPost/{{id}}">
    <div class="col-md-6">
      <h2>{{blog.title}}</h2>
    </div>
  </a>
  <div class="col-md-6">
    <p>{{blog.content}}</p>
  </div>
</div>
{{/each }}
